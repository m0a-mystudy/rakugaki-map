{
  "name": "rakugaki-map",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "dev:emulator": "firebase emulators:start --only firestore",
    "dev:with-emulator": "VITE_USE_FIRESTORE_EMULATOR=true vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "security:scan": "detect-secrets scan --exclude-files '\\.env\\.local$' --exclude-files 'node_modules/'",
    "security:gitleaks": "gitleaks detect --config .gitleaks.toml",
    "security:all": "npm run security:scan && npm run security:gitleaks",
    "precommit": "pre-commit run --all-files",
    "firestore:rules": "firebase deploy --only firestore:rules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@react-google-maps/api": "^2.20.7",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "firebase": "^11.9.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.3",
    "vite": "^5.4.10"
  }
}
